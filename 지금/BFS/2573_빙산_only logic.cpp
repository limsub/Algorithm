#include <iostream>
#include <vector>
#include <string>
#include <stack>
#include <sstream>
#include <algorithm>
using namespace std;
#include <queue>
#include <cstdio>
#include <set>
// #include <pair>


// 2573 빙산

// 1. 현재 빙산이 나누어졌는지 확인하는 함수 필요
//  - 살아있는 빙산 아무데서나 bfs 실행 (만약에 살아있는 빙산 없으면 모두 다 녹음)
//  - 살아있는 빙산 개수 
//  => 둘이 같으면 하나의 빙산. 다르면 빙산이 나누어짐. 아니면 빙산 모두 녹은 상태


// 2. 주변의 0의 개수를 세서 빙산 녹이기
//  - 1년 단위로 한 번씩 돌기
//  - 단순. 배열 모두 돌면서 인접한 0 개수 저장한 배열 또 만들면 됨.



int main() {
    ios::sync_with_stdio(0);
    cin.tie(0); 

    
    return 0;
}