#include <iostream>
#include <vector>
#include <string>
#include <stack>
#include <sstream>
#include <algorithm>
using namespace std;
#include <queue>
#include <cstdio>
#include <set>
#include <map>
// #include <pair>
// #include <bits/stdc++.h>


// 20366 같이 눈사람 만들래?

// 조건 1. : 아래 눈덩이 지름 >= 위 눈덩이 지름 -> 이건 크게 신경 쓸 필요 없다. 어차피 두개 뽑기만 하면 됨
// 조건 2. : 눈사람 키 차이가 가장 작아야 함.
// => (a+b) - (c+d) 가 최소인 a, b, c, d를 뽑아라.


// 수를 중복해서 선택할 수 없다...

// 시작 위치를 어떻게 정해야 할 지 모르겠네.
// 더 이상 탐색을 하지 않아도 되는 경우가 뭘까.
// 포인터 4개를 어떻게 움직여야 할지 감이 안온다...

// 다르게 생각하면, "두 종류의 두 값의 차이의 합이 최소인 경우..." - 이건 아닌거같아...
// "합 - 합이 최소인 경우...."

// a b c d - - - - - - 이렇게 시작하거나,
// a b - - - - - - c d 이렇게 시작하는게 최선인 것 같다.

// 포기. 도저히 모르겟음

// 투 포인터?


int main() {
    ios::sync_with_stdio(0);
    cin.tie(0); 


    

    return 0;
}